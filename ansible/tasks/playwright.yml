---
- name: Check if Playwright is installed
  ansible.builtin.command: "{{ ansible_env.HOME }}/.local/share/fnm/fnm exec --using={{ fnm_node_version }} -- npm list -g playwright"
  register: playwright_check
  failed_when: false
  changed_when: false

- name: Install Playwright
  ansible.builtin.command: "{{ ansible_env.HOME }}/.local/share/fnm/fnm exec --using={{ fnm_node_version }} -- npm install -g playwright"
  when: playwright_check.rc != 0

- name: Check if Playwright CLI is installed
  ansible.builtin.command: "{{ ansible_env.HOME }}/.local/share/fnm/fnm exec --using={{ fnm_node_version }} -- npm list -g @playwright/cli"
  register: playwright_cli_check
  failed_when: false
  changed_when: false

- name: Install Playwright CLI
  ansible.builtin.command: "{{ ansible_env.HOME }}/.local/share/fnm/fnm exec --using={{ fnm_node_version }} -- npm install -g @playwright/cli"
  when: playwright_cli_check.rc != 0

- name: Install Playwright system dependencies
  ansible.builtin.command: "{{ ansible_env.HOME }}/.local/share/fnm/fnm exec --using={{ fnm_node_version }} -- npx --yes playwright install-deps"
  become: true
  changed_when: false
  environment:
    FNM_DIR: "{{ ansible_env.HOME }}/.local/share/fnm"
    HOME: "{{ ansible_env.HOME }}"

- name: Install Playwright browsers
  ansible.builtin.command: "{{ ansible_env.HOME }}/.local/share/fnm/fnm exec --using={{ fnm_node_version }} -- npx --yes playwright install {{ item }}"
  loop: "{{ playwright_browsers }}"
  changed_when: false

- name: Download Playwright skill from GitHub
  ansible.builtin.command:
    cmd: "{{ playbook_dir }}/../scripts/download-playwright-skill.sh"
    creates: "{{ playbook_dir }}/../skills/playwright/SKILL.md"
